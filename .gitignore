# ---------------------------------------------------------------------------
# Personal data — NEVER commit. The Spotify export is private listening history.
# ---------------------------------------------------------------------------
Spotify Extended Streaming History/
my_spotify_data.zip
*.zip
data/
output/
*.parquet
*.duckdb
*.duckdb.wal
*.json
.cache/
.env

# Your hand-written answers to the Stage 2 review list — a list of artists you
# listen to. The tracked artist_overrides.example.csv documents the format.
artist_overrides.csv

# Which genres you want playlists for — a statement of personal taste, and one
# that deliberately departs from what the trend analysis found. The tracked
# playlist_overrides.example.csv documents the format.
playlist_overrides.csv

# ---------------------------------------------------------------------------
# Private working notes — the original build brief, kept local.
# ---------------------------------------------------------------------------
spotify-history-project-spec.md

# ---------------------------------------------------------------------------
# Python
# ---------------------------------------------------------------------------
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/

# ---------------------------------------------------------------------------
# OS / editor
# ---------------------------------------------------------------------------
.DS_Store
.idea/
.vscode/

# ---------------------------------------------------------------------------
# Explicit allowlist — tracked config that would otherwise be caught by *.json
#
# The blanket `*.json` above exists to keep the Spotify export out of git, but
# it also swallows ordinary project config. Re-include those by name only —
# never by a broad pattern, or the export starts leaking back in.
# ---------------------------------------------------------------------------
!.streamlit/config.toml
!.claude/settings.json

# Personal Claude overrides stay local.
.claude/settings.local.json

# Scratch worktrees are separate checkouts, not content of this one.
.claude/worktrees/
